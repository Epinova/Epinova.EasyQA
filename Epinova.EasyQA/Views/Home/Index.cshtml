@{
    ViewBag.Title = "Home Page";
}

<input type="search" id="tableFilterInput" placeholder="Søk...">
<table>
<thead>
    <tr>
        <th>Navn</th>
        <th>QA-type</th>
        <th>Publisert dato</th>
        <th>Gjennomført av</th>
        <th>Se på/endre</th>
    </tr>
</thead>
<tbody>
    @if (Model.Count == 0) { 
        <tr>
            <td colspan="5">Ingen QA-er er opprettet enda! På tide?</td>
        </tr>
    }
    
    @foreach (QaInstance qa in Model) { 
        <tr>
            <td>@qa.Name</td>
            <td>@qa.QaTypeName</td>
            <td>@qa.PublishedDate</td>
            <td>@qa.User</td>
            <td>@Html.ActionLink("Se på/Endre", "Edit", new { controller = "Qa", id = qa.Id })</td>
        </tr>
    }
</tbody>
</table>